import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.ialk.segin',
  appName: 'segin',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
